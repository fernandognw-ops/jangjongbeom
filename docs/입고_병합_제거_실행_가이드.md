# 입고 병합 제거 - 실행 가이드

## 현재 상태

- inventory_products: 480건 ✓
- inventory_inbound: 0건 (이전 업로드 시 duplicate 에러로 중단)
- inventory_outbound: 0건
- inventory_stock_snapshot: 0건

---

## 1. Supabase SQL Editor에서 마이그레이션 실행

1. [Supabase 대시보드](https://supabase.com/dashboard) → 프로젝트 선택
2. **SQL Editor** → **New query**
3. 아래 SQL 전체 복사 후 붙여넣기 → **Run**
4. `입고 unique 제약 제거 완료. 1 row = 1 입고 행.` 메시지 확인

```sql
-- scripts/migrate_inbound_no_merge.sql

-- 1. unique constraint 제거 (ALTER TABLE DROP CONSTRAINT)
ALTER TABLE inventory_inbound DROP CONSTRAINT IF EXISTS inventory_inbound_unique_pcode_date;
ALTER TABLE inventory_inbound DROP CONSTRAINT IF EXISTS inventory_inbound_product_code_inbound_date_key;
ALTER TABLE inventory_inbound DROP CONSTRAINT IF EXISTS inventory_inbound_product_code_inbound_date_sales_channel_key;

-- 2. unique index 제거 (CREATE UNIQUE INDEX로 생성된 것)
DROP INDEX IF EXISTS idx_inbound_upsert;
DROP INDEX IF EXISTS unique_inbound_record;

-- 3. 조회용 인덱스만 유지
CREATE INDEX IF NOT EXISTS idx_inv_inbound_date ON inventory_inbound(inbound_date DESC);
CREATE INDEX IF NOT EXISTS idx_inv_inbound_product ON inventory_inbound(product_code);
CREATE INDEX IF NOT EXISTS idx_inv_inbound_warehouse ON inventory_inbound(dest_warehouse);

SELECT '입고 unique 제약 제거 완료. 1 row = 1 입고 행.' AS status;
```

---

## 2. Production 웹에서 Excel 업로드

1. https://jangjongbeom.vercel.app 접속
2. **Excel 업로드** 영역에서 동일 생산수불현황 파일 선택

---

## 3. 업로드 전 검증 수치 확인

| 항목 | 기대값 |
|------|--------|
| rawdata | 480 |
| inbound | 172 |
| outbound | 2965 |
| stock | 414 |

수치가 일치하면 **DB 반영** 클릭.

---

## 4. 반영 후 검증

```bash
node scripts/run_outbound_migration.mjs --verify
```

또는 Production 화면에서 확인:
- KPI 수치
- 그래프
- 재고 분포

---

## 결과 보고 (작성용)

| 항목 | 결과 |
|------|------|
| **inbound 마이그레이션 실행 여부** | ☐ 완료 / ☐ 미실행 |
| **업로드 전 검증 수치** | rawdata __ / inbound __ / outbound __ / stock __ |
| **commit 성공 여부** | ☐ 성공 / ☐ 실패 |
| **최종 DB row 수** | products __ / inbound __ / outbound __ / stock __ |
| **화면 반영 결과** | quick API ☐ / KPI ☐ / 그래프 ☐ / 재고 분포 ☐ |
