# 데이터 적재 전 검증 리포트

**검증 일시**: 2025-03-17  
**테스트 파일**: `C:\Users\pc\Downloads\0318_생산수불현황.xlsx`  
**파일 수정일**: 2026-03-18 16:15

---

## 1. inventory_products row count 확인

| 테이블 | row 수 | 판단 |
|--------|--------|------|
| inventory_products | **0** | rawdata 먼저 적재 필요 |
| inventory_inbound | 0 | - |
| inventory_outbound | 0 | - |
| inventory_stock_snapshot | 0 | - |

**결론**: `inventory_products`가 0건이므로 **rawdata 시트를 먼저 적재**해야 합니다.  
`integrated_sync.py`는 rawdata → inventory_products → 입고/출고/재고 순으로 처리하므로, **한 번에 실행하면 rawdata가 자동으로 먼저 적재**됩니다.

---

## 2. --dry-run 실행 결과

### 2.1 입고/출고/재고 파싱 건수

| 시트 | 파싱 건수 | 비고 |
|------|-----------|------|
| rawdata | 480건 | inventory_products 적재 대상 |
| 입고 | 14건 | |
| 출고 | 14건 | |
| 재고 | 413건 | 엑셀 총합 205,586원 |

### 2.2 dest_warehouse 분포 (일반/쿠팡)

| 시트 | 전체 | 빈값→일반 | 쿠팡 | 일반 |
|------|------|-----------|------|------|
| 입고 | 14건 | 14건 | 0건 | 0건 |
| 출고 | 14건 | 0건 | 0건 | 14건 |
| 재고 | 413건 | 413건 | 0건 | 0건 |

- **입고**: 원본 `(빈값)` → `일반` 매핑 14건
- **출고**: 원본 `일반` → `일반` 유지 14건
- **재고**: 원본 `(빈값)` → `일반` 매핑 413건
- **쿠팡**: 0건 (이 파일에는 쿠팡 채널 데이터 없음)

### 2.3 날짜 파싱 결과

- **snapshot_date**: 파일 수정일 기준 `2026-03-18` 사용
- **inbound_date / outbound_date**: 엑셀 컬럼(입고일자, 출고일자)에서 파싱
- 당월 범위: `2026-03-01` ~ `2026-03-31` (당월만 적재)

### 2.4 빈값 매핑 건수

| 항목 | 건수 | 처리 |
|------|------|------|
| 입고 dest_warehouse 빈값 | 14건 | → 일반 |
| 재고 dest_warehouse 빈값 | 413건 | → 일반 |

---

## 3. --validate 실행 결과 (웹 vs Python 파싱 비교)

### 3.1 불일치 요약

| 구분 | 건수 | 설명 |
|------|------|------|
| [입고] 웹에만 있음 | 3,600건 이상 | 웹 파서가 더 많은 행 반환 |
| [입고] Python에만 있음 | 1건 이상 | Python 파서에만 있는 행 |
| [입고] 수량 불일치 | 2건 | 동일 키, 수량 값 차이 |

### 3.2 불일치 필드

- **product_code**: 동일
- **dest_warehouse**: 동일 (일반/쿠팡)
- **날짜 (inbound_date)**: 동일
- **quantity**: 2건에서 Python vs 웹 값 상이
  - 예: `('8809635658868', '일반', '2026-03-03')` Python=588705 vs 웹=54000
  - 예: `('8809635658899', '일반', '2026-03-03')` Python=290715 vs 웹=18000

### 3.3 원인 분석

| 원인 | 설명 |
|------|------|
| **헤더/행필터 차이** | 웹 파서와 Python 파서의 헤더 행 탐색, 데이터 시작 행, 빈 행 필터링 규칙이 다름 |
| **매핑/집계** | 수량 컬럼 매핑(입수량 vs 수량), 같은 날짜·품목의 집계 방식 차이 |

**권장**: 실제 DB 적재는 **Python(integrated_sync.py)** 기준으로 진행. 웹 업로드와 결과를 맞추려면 `src/lib/excelParser/`와 `integrated_sync.py`의 파싱 규칙을 통일하는 작업이 필요합니다.

---

## 4. inventory_stock_snapshot 중복 검증

### 4.1 (product_code, dest_warehouse, snapshot_date) 기준

| 항목 | 결과 |
|------|------|
| 원본 중복 건수 | **58건** |
| 중복 품목 예시 | 8809912473207, 8809912473191, 8809912474617 등 |
| 처리 | 집계 시 **수량 합산** 후 적재 → **유일키 보장됨** |

### 4.2 중복 상세 (일부)

```
('8809912473207', '일반', '2026-03-18'): 엑셀 행 [1, 30, 224]
('8809912473191', '일반', '2026-03-18'): 엑셀 행 [2, 31, 225]
...
→ 외 48건
```

동일 (product_code, dest_warehouse, snapshot_date)에 대해 엑셀에 여러 행이 있으나, 적재 시 **수량을 합산**하여 1건으로 upsert되므로 DB 제약 조건 위반 없음.

---

## 5. 최종 판단: DB 적재 진행 여부

### 5.1 적재 가능 조건

| 항목 | 상태 |
|------|------|
| inventory_products 0건 | ✅ rawdata 포함 한 번에 적재 가능 |
| 파싱/매핑 | ✅ Python 기준 정상 (입고 14, 출고 14, 재고 413) |
| dest_warehouse 매핑 | ✅ 빈값→일반 처리 완료 |
| 재고 중복 | ✅ 집계 후 유일키 보장 |

### 5.2 주의 사항

| 항목 | 내용 |
|------|------|
| 웹 vs Python 불일치 | 웹 업로드와 Python sync 결과가 다를 수 있음. **Python(integrated_sync)을 단일 출처로 사용** 권장 |
| 수량 불일치 2건 | 입고 시트 내 동일 키에 대해 Python/웹 수량 차이. Python 파싱 결과를 기준으로 적재 |

### 5.3 최종 결론

**✅ 실제 DB 적재를 진행해도 됩니다.**

- Python `integrated_sync.py` 기준 파싱·매핑이 정상 동작
- rawdata → inventory_products → 입고/출고/재고 순으로 한 번에 적재 가능
- 재고 중복은 집계 후 유일키로 보장됨

**실행 예시** (검증 후):
```bash
python scripts/integrated_sync.py "C:\Users\pc\Downloads\0318_생산수불현황.xlsx"
```

---

## 부록: 검증 명령어

```bash
# 1. DB 상태 확인
node scripts/verify-db.mjs

# 2. dry-run (파싱/매핑만)
python scripts/integrated_sync.py "경로/엑셀.xlsx" --dry-run

# 3. validate (웹 vs Python 비교)
python scripts/integrated_sync.py "경로/엑셀.xlsx" --validate
```
