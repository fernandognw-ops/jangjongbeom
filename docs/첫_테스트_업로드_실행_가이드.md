# 첫 테스트 업로드 실행 가이드 (웹 UI 승인 기반)

## 0. (선택) 0건 초기화

이미 데이터가 있는 경우, 첫 테스트를 위해 초기화하려면:

**Supabase SQL Editor** 에서 `scripts/supabase_reset_run.sql` 또는 `scripts/full_reset_inventory.sql` 실행.

---

## 1. inventory_upload_logs 테이블 생성

**Supabase Dashboard → SQL Editor** 에서 아래 SQL 실행:

```sql
-- scripts/create_inventory_upload_logs.sql 내용
CREATE TABLE IF NOT EXISTS inventory_upload_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  uploaded_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  uploaded_by TEXT DEFAULT 'web',
  source TEXT NOT NULL DEFAULT 'web',
  filename TEXT NOT NULL,
  snapshot_date TEXT,
  rawdata_count INTEGER NOT NULL DEFAULT 0,
  inbound_count INTEGER NOT NULL DEFAULT 0,
  outbound_count INTEGER NOT NULL DEFAULT 0,
  stock_count INTEGER NOT NULL DEFAULT 0,
  total_value NUMERIC(18,2) DEFAULT 0,
  general_count INTEGER NOT NULL DEFAULT 0,
  coupang_count INTEGER NOT NULL DEFAULT 0,
  status TEXT NOT NULL DEFAULT 'success',
  error_message TEXT
);

CREATE INDEX IF NOT EXISTS idx_upload_logs_at ON inventory_upload_logs(uploaded_at DESC);

SELECT 'inventory_upload_logs 테이블 생성 완료' AS status;
```

또는 `scripts/create_inventory_upload_logs.sql` 파일 전체 복사 후 Run.

---

## 2. /api/inventory/quick 0건 상태 확인

로컬 dev 서버 실행 후:

```bash
npm run dev
```

다른 터미널에서:

```bash
curl -s http://localhost:3007/api/inventory/quick | head -200
```

또는 브라우저에서 `http://localhost:3007/api/inventory/quick` 접속.

**기대 결과**: `productCount: 0`, `totalValue: 0` 또는 `products: []`

---

## 3. 테스트 파일 웹 업로드

1. `http://localhost:3007` 접속
2. **생산수불현황 업로드** 영역에 Excel 파일(.xlsx) 드래그 또는 선택
3. 서버 파싱 → 검증 결과 표시 대기

---

## 4. 업로드 전 검증 수치 정리

검증 완료 후 화면에 표시되는 수치를 아래 형식으로 기록:

| 항목 | 값 |
|------|-----|
| rawdata | 건 |
| inbound | 건 |
| outbound | 건 |
| stock | 건 |
| total value | 원 |
| 일반 | 건 |
| 쿠팡 | 건 |
| snapshot_date | (예: 2026-03-18) |

- dest_warehouse 오류 없음 확인
- **DB 반영** 버튼 활성화 확인

---

## 5. 수치 확인 후 commit 진행

- 이상 없으면 **DB 반영** 버튼 클릭
- 성공 메시지 확인 (입고 N건, 출고 N건, 재고 N건)

---

## 6. 반영 후 검증 결과 보고

### 6-1. Supabase SQL 검증

`scripts/verify_after_first_upload.sql` 실행 후 결과 기록:

| 테이블 | row 수 |
|--------|--------|
| inventory_products | |
| inventory_inbound | |
| inventory_outbound | |
| inventory_stock_snapshot | |
| inventory_upload_logs | (1건 이상) |

### 6-2. inventory_upload_logs 확인

```sql
SELECT id, uploaded_at, filename, snapshot_date, rawdata_count, inbound_count, outbound_count, stock_count, total_value, general_count, coupang_count, status
FROM inventory_upload_logs
ORDER BY uploaded_at DESC
LIMIT 5;
```

### 6-3. /api/inventory/quick 결과

```bash
curl -s http://localhost:3007/api/inventory/quick
```

- productCount > 0
- totalValue > 0

### 6-4. 대시보드 확인

- **KPI**: 품목 수, 재고 금액, 수량, SKU 정상 표시
- **그래프**: 카테고리별 출고 데이터
- **재고 분포**: 일반/쿠팡 분포 정상
