# 배포 실패 복구 보고

## 1. 실패 원인 (Vercel Build Logs 기준)

### 18f8ec4 배포 실패 (9BmxJuuuJ) - 실제 에러

| 구분 | 내용 |
|------|------|
| **에러 유형** | TypeScript / Import |
| **파일** | `src/app/api/production-sheet-validate/route.ts:11` |
| **에러 원문** | `Module '"@/lib/excelParser/classifier"' has no exported member 'toDestWarehouse'.` |
| **원인** | `classifier.ts`에 `toDestWarehouse` 미export (커밋 누락) |

**Import trace:**
- `production-sheet-validate/route.ts` → `toDestWarehouse` import
- `commitProductionSheet.ts` → `production-sheet-commit/route.ts` → `toDestWarehouse` import

---

### 이전 수정 (18f8ec4 이전)

| 구분 | 내용 |
|------|------|
| **원인 1** | TypeScript 타입 오류 |
| **파일** | `src/app/api/production-sheet-commit/route.ts:77` |
| **에러** | `Property 'destWarehouseValid' does not exist on type` |
| **원인** | `previewTokenStore.ts`의 `validation` 타입에 `destWarehouseValid` 누락 |

| 구분 | 내용 |
|------|------|
| **원인 2** | TypeScript 타입 오류 |
| **파일** | `src/lib/productionSheetParser.ts:40` |
| **에러** | `Cannot find name 'RawdataRow'` |
| **원인** | `RawdataRow` import 누락 |

| 구분 | 내용 |
|------|------|
| **원인 3** | API prerender 오류 |
| **파일** | `api/inventory/stock`, `api/inventory-diag`, `api/demand-forecast` 등 |
| **에러** | `Invalid supabaseUrl: Must be a valid HTTP or HTTPS URL` |
| **원인** | 빌드 시 Supabase env 미사용 API prerender 시도 → `dynamic = "force-dynamic"` 미설정 |

---

## 2. 수정한 파일

### ae5bf43 - toDestWarehouse export 추가

| 파일 | 수정 내용 |
|------|-----------|
| `src/lib/excelParser/classifier.ts` | `toDestWarehouse` 함수 export 추가, `classifyWarehouseGroup` deprecated 처리 |

### 18f8ec4 - 이전 수정

| 파일 | 수정 내용 |
|------|-----------|
| `src/lib/previewTokenStore.ts` | `validation` 타입에 `destWarehouseValid: boolean` 추가 |
| `src/lib/productionSheetParser.ts` | `RawdataRow` import 추가 |
| `src/app/api/inventory/stock/route.ts` | `export const dynamic = "force-dynamic"` 추가 |
| `src/app/api/inventory/sync-category/route.ts` | `export const dynamic = "force-dynamic"` 추가 |
| `src/app/api/inventory-diag/route.ts` | `export const dynamic = "force-dynamic"` 추가 |
| `src/app/api/demand-forecast/route.ts` | `export const dynamic = "force-dynamic"` 추가 |
| `src/app/api/debug-category/route.ts` | `export const dynamic = "force-dynamic"` 추가 |
| `src/app/api/inventory/data/route.ts` | `export const dynamic = "force-dynamic"` 추가 |
| `src/app/api/dashboard/route.ts` | `export const dynamic = "force-dynamic"` 추가 |

---

## 3. 수정 내용 요약

1. **previewTokenStore.ts**: `validation` 타입에 `destWarehouseValid` 누락 → 추가
2. **productionSheetParser.ts**: `RawdataRow` 미정의 → `@/lib/excelParser`에서 import
3. **API routes**: prerender 시 Supabase env 미사용 → `dynamic = "force-dynamic"`으로 prerender 비활성화

---

## 4. 로컬 build 성공 여부

✅ **성공** (`npm run build` exit code 0)

---

## 5. 재배포 필요 여부

⚠️ **필요** - `ae5bf43` 커밋 푸시 후 Vercel 자동 배포 대기

```bash
git push origin main
```
