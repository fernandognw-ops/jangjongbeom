# 입고 병합 제거 - 결과 보고

## 1. 기존 constraint / index

| 이름 | 정의 | 출처 |
|------|------|------|
| idx_inbound_upsert | UNIQUE (product_code, inbound_date) | create_inventory_tables_for_sync.sql |
| inventory_inbound_product_code_inbound_date_key | UNIQUE (product_code, inbound_date) | PostgreSQL 기본명 |
| inventory_inbound_product_code_inbound_date_sales_channel_key | UNIQUE (product_code, inbound_date, sales_channel) | drop_inbound_sales_channel 이전 |
| **inventory_inbound_unique_pcode_date** | UNIQUE (product_code, inbound_date) | **에러 발생 제약** |
| unique_inbound_record | UNIQUE | rollback_unique_constraints.sql |
| idx_inbound_upsert (migrate_inbound_outbound) | UNIQUE (product_code, inbound_date, dest_warehouse) | migrate_inbound_outbound_unique_for_row_count.sql |

---

## 2. 제거 여부

| 항목 | 유형 | 제거 방법 |
|------|------|-----------|
| inventory_inbound_unique_pcode_date | constraint | ALTER TABLE DROP CONSTRAINT |
| inventory_inbound_product_code_inbound_date_key | constraint | ALTER TABLE DROP CONSTRAINT |
| inventory_inbound_product_code_inbound_date_sales_channel_key | constraint | ALTER TABLE DROP CONSTRAINT |
| idx_inbound_upsert | index | DROP INDEX |
| unique_inbound_record | index | DROP INDEX |

---

## 3. 추가 인덱스

| 인덱스 | 컬럼 | 용도 |
|--------|------|------|
| idx_inv_inbound_date | inbound_date DESC | 날짜별 조회 |
| idx_inv_inbound_product | product_code | 품목별 조회 |
| idx_inv_inbound_warehouse | dest_warehouse | 창고별 조회 |

---

## 4. 마이그레이션 파일

**파일명**: `scripts/migrate_inbound_no_merge.sql`

**실행**: Supabase SQL Editor에서 내용 붙여넣기 후 Run

---

## 5. 수정 후 기대 row 수

| 테이블 | 기대값 |
|--------|--------|
| inventory_inbound | 172 |

---

## 6. validate / commit 로직

| 항목 | 상태 |
|------|------|
| validate | inboundCount = inbound.length (원본 기준) ✓ |
| commit | 1 row = 1 insert, 병합 없음 ✓ |
| create_inventory_tables_for_sync.sql | unique index 제거, 조회용 인덱스만 유지 ✓ |

---

## 7. 실행 순서

1. `scripts/migrate_inbound_no_merge.sql` 실행
2. 동일 Excel 웹 업로드 → 검증 (inbound 172) → DB 반영
3. `node scripts/run_outbound_migration.mjs --verify` 로 확인
